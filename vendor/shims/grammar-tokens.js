(function() {
  function vendorModule() {
    'use strict';

    return {
      'default': self['grammar-tokens'],
      __esModule: true,
    };
  }

  define('grammar-tokens', [], vendorModule);
})();
